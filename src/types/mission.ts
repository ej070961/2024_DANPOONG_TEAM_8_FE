export interface ActivatedMission {
  id: number;
  missionType: string;
  missionTitle: string;
  isComplete: boolean;
}

export interface CompletedMission {
  id: number;
  missionType: string;
  missionTitle: string;
  isComplete: boolean;
}
